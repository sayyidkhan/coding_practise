function isPalindrome(number) {

        let originalNumber = number;
        let reverse = 0;
		let digit = 0;
        while(number != 0) {
            digit = number % 10;
            reverse = (reverse * 10) + digit;
            number /= 10;
			number = parseInt(number);
        }
        return reverse;
}
